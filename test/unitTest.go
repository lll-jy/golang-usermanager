package main

import (
	"testing"
	//"./../cmd/main"
	//"/cmd/main"
	"git.garena.com/jiayu.li/entry-task/cmd/usermanager"
)

func Test_valid_username(t *testing.T) {
	// want := true
	// if got := cmd.isValidUsername("abcdefg"); got != want {
	// 	t.Errorf("isValidUsername(%s) = %t, want %t", got, want)
	// }
}
